import config from "../../config.json";

export const LINKS = {
    home: `${config.baseUrl}/`,
    admin: `${config.baseUrl}/admin`,
    standUpWheel: `${config.baseUrl}/wheel`,
    vibeCheck: `${config.baseUrl}/vibecheck`,
    wheelStats: `${config.baseUrl}/stats/wheel`,
    vibeCheckStats: `${config.baseUrl}/stats/vibecheck`
}